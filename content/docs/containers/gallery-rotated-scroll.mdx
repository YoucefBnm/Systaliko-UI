---
title: Rotated Gallery On Scroll
description: Animated gallery images, with scroll triggered animations, rotates and translates on the Y axis on scroll.
---

<ComponentPreview name="gallery-rotated-scroll-demo" />

## Installation

<ComponentInstallation name="gallery-rotated-scroll" />

## Usage

```tsx
<GalleryRotatedScroll spacerClass="h-96">
  <div
    className="pointer-events-none absolute z-10 h-[20vh] w-full "
    style={{
      background: 'linear-gradient(to right, gray, rebeccapurple, blue)',
      filter: 'blur(84px)',
      mixBlendMode: 'screen',
    }}
  />

  <GalleryContainer className="h-dvh  gap-2">
    <GalleryCol className="-mt-2 gap-2">
      {IMAGES_1.map((imageUrl, index) => (
        <img
          key={index}
          className="aspect-video block h-auto max-h-full w-full  rounded-md  object-cover shadow"
          src={imageUrl}
          alt="gallery item"
        />
      ))}
    </GalleryCol>
    <GalleryCol className="gap-2 mt-[-20%]" yRange={['15%', '5%']}>
      {IMAGES_2.map((imageUrl, index) => (
        <img
          key={index}
          className="aspect-video block h-auto max-h-full w-full  rounded-md  object-cover shadow"
          src={imageUrl}
          alt="gallery item"
        />
      ))}
    </GalleryCol>
    <GalleryCol yRange={['-10%', '2%']} className="gap-2 -mt-2">
      {IMAGES_3.map((imageUrl, index) => (
        <img
          key={index}
          className="aspect-video block h-auto max-h-full w-full  rounded-md  object-cover shadow"
          src={imageUrl}
          alt="gallery item"
        />
      ))}
    </GalleryCol>
  </GalleryContainer>
</GalleryRotatedScroll>
```

## Examples

<ComponentPreview name="device-rotated-scroll-demo" />

## Props

#### GalleryRotatedScroll

<TypeTable
  type={{
    spacerClass: {
      description: 'the class to be applied to the spacer element on bottom',
      type: 'string',
      required: false,
    },
  }}
/>
#### GalleryContainer
<TypeTable
  type={{
    yRange: {
      description: 'the range of the y-axis',
      type: 'number',
      required: false,
      default: '[0, 300]',
    },
    rotateXRange: {
      description: 'the range of the rotateX-axis',
      type: 'number',
      required: false,
      default: '[80, 0]',
    },
    scaleRange: {
      description: 'the range of the scale-axis',
      type: 'number',
      required: false,
      default: '[1.1, 1]',
    },
  }}
/>

#### GalleryCol

<TypeTable
  type={{
    yRange: {
      description: 'the range of the y-axis',
      type: 'number',
      required: false,
      default: '[-5%, 1%]',
    },
  }}
/>
