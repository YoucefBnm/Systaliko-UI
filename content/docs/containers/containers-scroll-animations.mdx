---
title: Scroll Trigger Animations
description: Scroll triggerd animations never been easier, now you can animate transforms, and roundness and positioning.
new: true
---

### Combo animations (inset on x axis, scale and translateY)

This demo is inspired by [Beyond agency](https://bynd-agency.com/) hero section.

<ComponentPreview name="container-scroll-combo-demo" />

## Installation

<ComponentInstallation name="containers-scroll-animations" />

## Usage

```tsx
<ContainerScrollAnimation className="overflow-hidden">
  <ContainerScrollTranslate className="h-dvh relative">
    <ContainerScrollInset className="h-full relative">
      <ContainerScrollScale className="flex bg-secondary gap-2 overflow-hidden px-6">
        <ContainerScrollTranslate
          yRange={['0%', '-10%']}
          className="flex flex-col gap-2"
        >
          {IMAGES_1.map((imageUrl, index) => (
            <img
              key={index}
              className="aspect-[4/2.5] inline-block align-middle h-auto max-h-full w-full  object-cover"
              src={imageUrl}
              alt="gallery item"
            />
          ))}
        </ContainerScrollTranslate>

        <ContainerScrollTranslate
          yRange={['0%', '20%']}
          className="flex mt-[-20%] relative flex-col gap-2"
        >
          {IMAGES_2.map((imageUrl, index) => (
            <img
              key={index}
              className="aspect-[4/2.5] inline-block align-middle h-auto max-h-full w-full  object-cover"
              src={imageUrl}
              alt="gallery item"
            />
          ))}
        </ContainerScrollTranslate>

        <ContainerScrollTranslate
          yRange={['0%', '-10%']}
          className="hidden md:flex flex-col gap-2"
        >
          {IMAGES_3.map((imageUrl, index) => (
            <img
              key={index}
              className="aspect-[4/2.5] inline-block align-middle h-auto max-h-full w-full  object-cover"
              src={imageUrl}
              alt="gallery item"
            />
          ))}
        </ContainerScrollTranslate>
      </ContainerScrollScale>
    </ContainerScrollInset>
  </ContainerScrollTranslate>
</ContainerScrollAnimation>
```

## Examples

### Inset with roundness

<ComponentPreview name="container-scroll-inset-demo" />

### Inset-X

<ComponentPreview name="container-scroll-inset-x-demo" />

### Inset-Y

<ComponentPreview name="container-scroll-inset-y-demo" />

### Roundness

<ComponentPreview name="container-scroll-radius-demo" />

### Scale

<ComponentPreview name="container-scroll-scale-demo" />

## Props

#### ContainerScrollAnimation

<TypeTable
  type={{
    spacerClass: {
      description: 'the class to be applied to the spacer element on bottom',
      type: 'string',
      required: false,
    },
  }}
/>

#### ContainerScrollTranslate

<TypeTable
  type={{
    inputRange: {
      description: 'The range of the scroll range minimum 0 and max 1.',
      type: 'number[]',
      required: false,
      default: '[0, 1]',
    },
    yRange: {
      description: 'Animation range of the y transform.',
      type: 'number[]',
      required: false,
      default: '[0, 384]',
    },
  }}
/>

#### ContainerScrollInset

<TypeTable
  type={{
    inputRange: {
      description: 'The range of the scroll range minimum 0 and max 1.',
      type: 'number[]',
      required: false,
      default: '[0, 1]',
    },
    insetRange: {
      description: 'Animation range of the inset on x axis.',
      type: 'number[]',
      required: false,
      default: '[48, 0]',
    },
  }}
/>

#### ContainerScrollScale

<TypeTable
  type={{
    inputRange: {
      description: 'The range of the scroll range minimum 0 and max 1.',
      type: 'number[]',
      required: false,
      default: '[0, 1]',
    },
    yRange: {
      description: 'Animation range of the scale transform.',
      type: 'number[]',
      required: false,
      default: '[1.2, 1]',
    },
  }}
/>
