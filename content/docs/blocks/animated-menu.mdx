---
title: Animated Menu
description: Animated Menu component with orchestrated children animation, fully customizable.
---

<ComponentPreview name="animated-menu-demo" />

## Installation

<ComponentInstallation name="animated-menu" />

## Usage

```tsx
<AnimatedMenu>
  <AnimatedMenuButton>
    <AnimatedMenuButtonToggleIcon />
    <AnimatedMenuButtonLabel />
  </AnimatedMenuButton>

  <AnimatedMenuList>
    <div className="flex flex-col items-start gap-4 *:transition-blur *:duration-300 [&:hover>*]:blur-[2px] [&>*:hover]:blur-none">
      {menuItems.map((item, i) => (
        <AnimatedMenuItem key={i} order={i}>
          <Link
            className="text-sm font-medium text-muted"
            href={item.href}
            title={item.title}
          >
            {item.title}
          </Link>
        </AnimatedMenuItem>
      ))}
    </div>
  </AnimatedMenuList>
</AnimatedMenu>
```

## Examples

### with different reveal variants

<ComponentPreview name="animated-menu-2-demo" />

## Props

`AnimatedMenuButtonToggleIcon`

<TypeTable
  type={{
    size: {
      description: 'size of the hamburger icon.',
      type: '"sm" | "md" | "lg" | "xl" | null',
      required: false,
      default: 'sm',
    },
  }}
/>

`AnimatedMenuButtonLabel`

<TypeTable
  type={{
    closeLabel: {
      description: 'label showed when menu is closed.',
      type: 'string',
      required: false,
      default: 'menu',
    },
    openLabel: {
      description: 'label showed when menu is open.',
      type: 'string',
      required: false,
      default: 'close',
    },
  }}
/>

`AnimatedMenuList`

<TypeTable
  type={{
    menuListVariants: {
      description: 'motion variants of open and close states.',
      type: 'Variants',
      required: false,
      default: 'variants',
    },
  }}
/>

`AnimatedMenuItem`

<TypeTable
  type={{
    menuListVariants: {
      description:
        'motion reveal variants of the menu item at initial and enter and exit states.',
      type: 'Variants',
      required: false,
      default: 'itemVariants',
    },
  }}
/>
